import React from 'react';

import { Checkout } from '@/components/checkout/Checkout';

const CheckoutPage = () => {
	return (
		<main className=' relative inset-0 z-0 bg-rose'>
			<Checkout />
		</main>
	);
};

export default CheckoutPage;
